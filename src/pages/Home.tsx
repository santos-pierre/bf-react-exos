import BaseLayout from '../Layouts/BaseLayout';

const Home = () => {
    return (
        <BaseLayout>
            <h1 className="font-bold text-7xl py-5 text-center underline decoration-react  uppercase hover:decoration-gray-200 hover:text-react select-none transition-all duration-500">
                BF Exercices Reacts
            </h1>
        </BaseLayout>
    );
};

export default Home;
